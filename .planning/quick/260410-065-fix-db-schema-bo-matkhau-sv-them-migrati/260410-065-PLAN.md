---
quick_id: 260410-065
type: quick
autonomous: true
files_modified:
  - backend/database/migrations/2026_04_05_000002_create_sinhvien_table.php
  - backend/database/migrations/2026_04_09_000010_create_topic_registrations_form_table.php
  - backend/app/Models/SinhVien.php
  - backend/app/Models/TopicRegistrationForm.php
  - backend/app/Http/Controllers/TopicRegistrationFormController.php
  - backend/routes/api.php
  - frontend/src/App.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/pages/admin/PhanCong.jsx
  - frontend/src/pages/admin/NhapLieu.jsx
  - frontend/src/services/nhapLieuService.js
---

<objective>
Cập nhật 4 thứ: (1) bỏ cột matKhau khỏi bảng sinhvien, (2) thêm migration bảng topic_registrations_form, (3) gộp 2 trang PhanCongGVHD + PhanCongGVPB thành 1 trang PhanCong, (4) thêm trang NhapLieu với CRUD và import Excel.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: BE — Bỏ matKhau khỏi sinhvien, thêm migration topic_registrations_form</name>
  <files>
    backend/database/migrations/2026_04_05_000002_create_sinhvien_table.php
    backend/database/migrations/2026_04_09_000010_create_topic_registrations_form_table.php
    backend/app/Models/SinhVien.php
    backend/app/Models/TopicRegistrationForm.php
    backend/app/Http/Controllers/SinhVienController.php
    backend/app/Http/Controllers/TopicRegistrationFormController.php
    backend/routes/api.php
  </files>
  <action>
1. Sửa migration sinhvien (2026_04_05_000002): xóa dòng `$table->string('matKhau', 255)`.

2. Sửa SinhVien model: bỏ 'matKhau' khỏi $fillable, bỏ $hidden, bỏ hàm getAuthPassword(). Giữ nguyên extends Authenticatable và HasApiTokens (vẫn dùng cho GV, nhưng SinhVien model có thể cần nếu sau này cần). Thực ra SV không login nên có thể đổi SinhVien extends Model thay vì Authenticatable — làm luôn.

3. Sửa SinhVienController: bỏ `use Hash`, bỏ `Hash::make($mssv)` trong hàm import() và store(). Bỏ trường matKhau khỏi SinhVien::create().

4. Tạo migration mới `2026_04_09_000010_create_topic_registrations_form_table.php`:
```php
Schema::create('topic_registrations_form', function (Blueprint $table) {
    $table->id();
    $table->string('topic_title', 255);
    $table->text('topic_description')->nullable();
    $table->enum('topic_type', ['mot_sinh_vien', 'hai_sinh_vien']);
    $table->string('student1_id', 20);
    $table->string('student1_name', 255);
    $table->string('student1_class', 50);
    $table->string('student1_email', 255)->nullable();
    $table->string('student2_id', 20)->nullable();
    $table->string('student2_name', 255)->nullable();
    $table->string('student2_class', 50)->nullable();
    $table->string('student2_email', 255)->nullable();
    $table->string('gvhd_code', 20)->nullable();
    $table->string('gvhd_workplace', 255)->nullable();
    $table->string('gvpb_code', 20)->nullable();
    $table->text('note')->nullable();
    $table->string('source', 50)->default('google_form');
    $table->enum('status', ['cho_duyet', 'da_duyet', 'tu_choi'])->default('cho_duyet');
    $table->timestamp('registered_at')->useCurrent();
    $table->timestamp('updated_at')->useCurrent()->useCurrentOnUpdate();

    $table->foreign('gvhd_code')->references('maGV')->on('giangvien')->nullOnDelete()->cascadeOnUpdate();
});
```

5. Tạo Model `TopicRegistrationForm.php`:
- $table = 'topic_registrations_form'
- $fillable: tất cả các cột ngoài id
- $timestamps = false (dùng registered_at/updated_at thủ công)
- Quan hệ: gvhd() belongsTo GiangVien, foreignKey 'gvhd_code', ownerKey 'maGV'

6. Tạo `TopicRegistrationFormController.php` với các method:
- index(): trả về paginate(20), hỗ trợ filter ?search= (MSSV/tên SV), ?status=
- store(): validate rồi tạo bản ghi mới. Sau khi tạo, kiểm tra student1_id và student2_id, nếu chưa có trong bảng sinhvien thì insert (chỉ mssv, hoTen, lop, email). Dùng SinhVien::firstOrCreate(['mssv' => ...], [...]).
- update(): validate rồi update
- destroy(): xóa bản ghi

7. Thêm vào routes/api.php (trong middleware auth:sanctum):
```php
Route::apiResource('nhap-lieu', TopicRegistrationFormController::class);
Route::post('nhap-lieu/import-excel', [TopicRegistrationFormController::class, 'importExcel']);
```

8. Thêm method importExcel() vào TopicRegistrationFormController:
- Validate file xlsx/xls
- Dùng IOFactory::load() đọc spreadsheet
- Đọc sheet "DSSV_ĐK_HƯỚNG ĐỀ TÀI": dòng đầu là header, tìm cột MSSV, Họ tên, Lớp theo tên cột (iterate qua row[0] để tìm index)
- Đọc sheet "SVĐK_TheoLink": tương tự
- Với mỗi row hợp lệ: tạo bản ghi topic_registrations_form với source='excel_import', status='cho_duyet'. topic_title lấy từ cột tên đề tài nếu có, không thì để 'Chưa có tiêu đề'. student1_id, student1_name, student1_class từ dữ liệu đọc được.
- Sau đó SinhVien::firstOrCreate cho student1_id.
- Trả về { imported, errors }

Lưu ý: SinhVienController sau khi bỏ matKhau thì `use Hash` không cần nữa, xóa luôn import đó.
  </action>
  <verify>
php artisan migrate:fresh --seed (chạy trong backend/) không báo lỗi. GET /api/nhap-lieu trả về 200.
  </verify>
  <done>
- Migration sinhvien không còn cột matKhau
- Bảng topic_registrations_form tồn tại với đúng schema
- CRUD /api/nhap-lieu hoạt động
- Import Excel endpoint tồn tại
  </done>
</task>

<task type="auto">
  <name>Task 2: FE — Gộp PhanCong, thêm trang NhapLieu</name>
  <files>
    frontend/src/App.jsx
    frontend/src/components/Sidebar.jsx
    frontend/src/pages/admin/PhanCong.jsx
    frontend/src/pages/admin/NhapLieu.jsx
    frontend/src/services/nhapLieuService.js
  </files>
  <action>
1. Tạo `frontend/src/pages/admin/PhanCong.jsx`:
Gộp nội dung của PhanCongGVHD và PhanCongGVPB thành 1 trang. Dùng 2 tab: "Phân công GVHD" và "Phân công GVPB". Hiện tại cả 2 đều là placeholder → trang PhanCong mới cũng là placeholder với 2 tab switch đơn giản bằng useState. Mỗi tab hiển thị "Chức năng đang phát triển..." text.

2. Xóa logic của `PhanCongGVHD.jsx` và `PhanCongGVPB.jsx` — giữ nguyên 2 file nhưng chỉ export component rỗng (hoặc redirect sang /admin/phan-cong), hoặc không cần giữ nếu App.jsx bỏ route đó. Cách đơn giản hơn: giữ file nhưng component trả về null (để tránh lỗi import nếu file vẫn được import). Thực ra tốt hơn là xóa 2 import cũ trong App.jsx và thêm PhanCong mới.

3. Tạo `frontend/src/services/nhapLieuService.js`:
```js
import api from './api';

export function getList(params) {
  return api.get('/nhap-lieu', { params });
}

export function createRecord(data) {
  return api.post('/nhap-lieu', data);
}

export function updateRecord(id, data) {
  return api.put(`/nhap-lieu/${id}`, data);
}

export function deleteRecord(id) {
  return api.delete(`/nhap-lieu/${id}`);
}

export function importExcel(formData) {
  return api.post('/nhap-lieu/import-excel', formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
  });
}
```

(Kiểm tra xem file api.js/api.jsx trong services tên gì trước khi import — đọc services/ folder để dùng đúng tên)

4. Tạo `frontend/src/pages/admin/NhapLieu.jsx`:

Layout chính:
- Header: tiêu đề "Nhập liệu đăng ký đề tài", nút "Thêm mới" và nút "Import Excel"
- Bảng dữ liệu: cột STT, Tiêu đề đề tài, Loại (1 SV / nhóm 2 SV), MSSV SV1, Họ tên SV1, Lớp, GVHD, Trạng thái, Nguồn, Hành động (Sửa | Xóa | Thêm vào đề tài chính)
- Pagination (dùng dữ liệu từ Laravel paginate)
- Ô tìm kiếm search

Modal thêm/sửa:
- Form fields: topic_title, topic_type (select: 1 SV / 2 SV), student1_id, student1_name, student1_class, student1_email, student2_id (hiện khi type = 2 SV), student2_name, student2_class, student2_email, gvhd_code, gvhd_workplace, note, status
- Nút Submit tạo/cập nhật

Modal import Excel:
- Input file chọn file .xlsx/.xls
- Nút "Import"
- Sau import hiển thị kết quả: đã import X bản ghi, Y lỗi

Nút "Thêm vào đề tài chính": disabled hoặc onClick={() => alert('Chức năng sắp có')}

Dùng useState cho modal open/close, dữ liệu form, loading state. Dùng useEffect để fetch dữ liệu khi mount và khi search thay đổi (debounce đơn giản hoặc chỉ fetch khi bấm Enter/button).

Styling: Tailwind CSS, nhất quán với các trang khác trong project (xem SinhVien.jsx để tham khảo style table).

5. Sửa `frontend/src/App.jsx`:
- Bỏ import PhanCongGVHD và PhanCongGVPB
- Thêm import PhanCong from './pages/admin/PhanCong'
- Thêm import NhapLieu from './pages/admin/NhapLieu'
- Thay route /admin/phan-cong-gvhd và /admin/phan-cong-gvpb bằng route /admin/phan-cong
- Thêm route /admin/nhap-lieu

6. Sửa `frontend/src/components/Sidebar.jsx` (menuConfig.admin):
- Bỏ entry { label: 'Phân công GVHD', path: '/admin/phan-cong-gvhd', ... }
- Bỏ entry { label: 'Phân công GVPB', path: '/admin/phan-cong-gvpb', ... }
- Thêm { label: 'Phân công GVHD/GVPB', path: '/admin/phan-cong', icon: HiOutlineUserGroup }
- Thêm { label: 'Nhập liệu', path: '/admin/nhap-lieu', icon: HiOutlinePencilSquare } (đặt sau Sinh viên hoặc sau Giảng viên cho hợp lý về flow)

Thứ tự menu admin mới gợi ý: Tổng quan → Sinh viên → Giảng viên → Nhập liệu → Phân công GVHD/GVPB → Đề tài → Hội đồng → Điểm tổng kết → Cài đặt
  </action>
  <verify>
npm run dev trong frontend/ không báo lỗi compile. Truy cập /admin/phan-cong thấy trang 2 tab. Truy cập /admin/nhap-lieu thấy bảng và các nút.
  </verify>
  <done>
- Sidebar không còn "Phân công GVHD" và "Phân công GVPB" riêng lẻ, chỉ còn "Phân công GVHD/GVPB"
- Route /admin/phan-cong tồn tại với 2 tab
- Route /admin/nhap-lieu tồn tại với bảng CRUD và import Excel
- Không còn route /admin/phan-cong-gvhd và /admin/phan-cong-gvpb
  </done>
</task>

</tasks>

<success_criteria>
- Bảng sinhvien không có cột matKhau sau migrate:fresh
- Bảng topic_registrations_form tồn tại đúng schema
- API CRUD /api/nhap-lieu và /api/nhap-lieu/import-excel hoạt động
- FE compile không lỗi, menu Sidebar cập nhật đúng
- Trang /admin/phan-cong có 2 tab GVHD và GVPB
- Trang /admin/nhap-lieu có bảng + modal thêm/sửa + import Excel
</success_criteria>
