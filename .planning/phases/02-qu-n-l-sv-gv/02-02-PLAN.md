---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/pages/admin/SinhVien.jsx
  - frontend/src/pages/admin/GiangVien.jsx
  - frontend/src/pages/admin/CaiDat.jsx
  - frontend/src/services/sinhVienService.js
  - frontend/src/services/giangVienService.js
  - frontend/src/services/kyLvtnService.js
  - frontend/package.json
autonomous: false
requirements:
  - SV-01
  - SV-02
  - SV-03
  - GV-01
  - GV-02
  - KY-01
  - KY-02
  - KY-03
must_haves:
  truths:
    - "Admin upload file Excel tren trang SinhVien -> SV xuat hien trong bang"
    - "Loi import hien thi theo dong tren UI (row number + message)"
    - "Admin search/filter SV theo lop, ten, MSSV"
    - "Admin filter SV theo GVHD da phan cong (dropdown, optional)"
    - "Admin them/sua/xoa GV qua modal form"
    - "Danh sach GV hien so SV HD (x/10), so DT PB, so HD"
    - "Admin tao/sua ky LVTN voi ten va 6 moc ngay"
    - "Form CRUD SV/GV hien thi loi validation inline duoi field, khong dung alert"
  artifacts:
    - path: "frontend/src/pages/admin/SinhVien.jsx"
      provides: "Trang sinh vien voi table, import modal, CRUD modal"
    - path: "frontend/src/pages/admin/GiangVien.jsx"
      provides: "Trang giang vien voi table, CRUD modal, counts"
    - path: "frontend/src/pages/admin/CaiDat.jsx"
      provides: "Trang cai dat ky LVTN voi form tao/sua"
    - path: "frontend/src/services/sinhVienService.js"
      provides: "API functions: getStudents, importStudents, createStudent, updateStudent, deleteStudent"
    - path: "frontend/src/services/giangVienService.js"
      provides: "API functions: getLecturers, createLecturer, updateLecturer, deleteLecturer"
    - path: "frontend/src/services/kyLvtnService.js"
      provides: "API functions: getKyLvtn, createKyLvtn, updateKyLvtn"
  key_links:
    - from: "SinhVien.jsx"
      to: "/api/students, /api/students/import"
      via: "sinhVienService.js -> api.js"
    - from: "GiangVien.jsx"
      to: "/api/giang-vien"
      via: "giangVienService.js -> api.js"
    - from: "CaiDat.jsx"
      to: "/api/ky-lvtn"
      via: "kyLvtnService.js -> api.js"
---

# Plan 02-02: Frontend — Trang SinhVien, GiangVien, CaiDat Ky LVTN

## Objective

Thay the 3 placeholder pages (SinhVien, GiangVien, CaiDat) bang cac trang co chuc nang day du: import SV tu Excel voi hien thi loi, bang danh sach SV/GV voi filter/search/pagination, modal them/sua/xoa, va form quan ly ky LVTN.

Purpose: Hoan thanh toan bo giao dien Phase 2 cho admin.
Output: 3 trang React hoan chinh, 3 service files.

## Execution Context

@.planning/phases/02-qu-n-l-sv-gv/02-RESEARCH.md
@.planning/phases/02-qu-n-l-sv-gv/02-UI-SPEC.md
@.planning/phases/01-n-n-t-ng/01-02-fe-nen-tang-SUMMARY.md

## Context

@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

### Interfaces (tu Phase 1 + Plan 02-01)

```javascript
// frontend/src/services/api.js (da co)
import axios from 'axios';
const api = axios.create({ baseURL: '/api', headers: { 'Content-Type': 'application/json' } });
// interceptor: Bearer token tu localStorage, 401 redirect /login
export default api;

// frontend/src/context/AuthContext.jsx (da co)
export function useAuth() { return { user, token, saveAuth, clearAuth }; }

// API endpoints (tu Plan 02-01):
// POST   /api/students/import    multipart/form-data { file, ky_lvtn_id } -> { imported, errors[] }
// GET    /api/students           ?ky_id=&lop=&gvhd_id=&search=&page= -> paginated { data, total, per_page, current_page }
// POST   /api/students           { mssv, hoTen, lop, email, ky_lvtn_id } -> student
// PUT    /api/students/{mssv}    { hoTen, lop, email, ky_lvtn_id } -> student
// DELETE /api/students/{mssv}    -> { message }
// GET    /api/giang-vien         -> { data: [{maGV, tenGV, email, hocVi, isAdmin, so_sv_hd, so_dt_pb, so_hd}] }
// POST   /api/giang-vien         { maGV, tenGV, email, password, hocVi?, isAdmin? } -> gv
// PUT    /api/giang-vien/{maGV}  { tenGV, email, password?, hocVi?, isAdmin? } -> gv
// DELETE /api/giang-vien/{maGV}  -> { message }
// GET    /api/ky-lvtn            -> { data: [{id, ten, is_active, ngay_*}] }
// POST   /api/ky-lvtn            { ten, ngay_* } -> ky
// PUT    /api/ky-lvtn/{id}       { ten, ngay_* } -> ky
```

## Tasks

<task type="auto" id="02-02-01">
  <name>Task 1: Install react-table + Service files + Trang SinhVien</name>
  <files>
    frontend/package.json,
    frontend/src/services/sinhVienService.js,
    frontend/src/services/giangVienService.js,
    frontend/src/services/kyLvtnService.js,
    frontend/src/pages/admin/SinhVien.jsx
  </files>
  <read_first>
    - frontend/package.json -- xem dependencies hien tai
    - frontend/src/services/api.js -- pattern axios instance, interceptor, DEFAULT HEADERS
    - frontend/src/services/authService.js -- pattern service file
    - frontend/src/pages/admin/SinhVien.jsx -- hien tai la placeholder
    - frontend/src/pages/PlaceholderPage.jsx -- pattern page component
    - frontend/src/context/AuthContext.jsx -- useAuth hook
    - frontend/src/App.jsx -- route /admin/sinh-vien da co
    - .planning/phases/02-qu-n-l-sv-gv/02-UI-SPEC.md -- visual specs
    - .planning/phases/02-qu-n-l-sv-gv/02-RESEARCH.md -- FE patterns
  </read_first>
  <action>
    A. Cai @tanstack/react-table: `cd frontend && npm install @tanstack/react-table@^8`

    B. Tao 3 service files theo pattern cua authService.js. Moi file import api tu './api' va export cac functions:

    1. sinhVienService.js: getStudents(params), importStudents(file, kyLvtnId), createStudent(data), updateStudent(mssv, data), deleteStudent(mssv)

       QUAN TRONG cho importStudents: Phai set `headers: { 'Content-Type': undefined }` trong options cua api.post(). Day la exception hop ly vi api.js da set default `Content-Type: 'application/json'`. Khi gui FormData (multipart), phai xoa header nay de axios tu set `multipart/form-data` voi boundary tu dong. Neu khong lam dieu nay, server se nhan header `application/json` va khong parse duoc file upload. Day KHONG vi pham Pitfall 6 cua RESEARCH.md ("khong set header thu cong") — vi o day ta dang XOA default header de axios tu xu ly, khong phai set 1 Content-Type cu the.

    2. giangVienService.js: getLecturers(), createLecturer(data), updateLecturer(maGV, data), deleteLecturer(maGV)

    3. kyLvtnService.js: getKyLvtn(), createKyLvtn(data), updateKyLvtn(id, data)

    C. Viet lai SinhVien.jsx tu placeholder thanh trang day du. Tham khao UI-SPEC.md cho Tailwind classes chi tiet.

    Component structure:
    - State: showImportModal, showFormModal, editItem, deleteItem, showDeleteConfirm
    - State cho filter: kyId, lop, gvhdId (optional dropdown — SV-03 filter theo GVHD), search (debounced)
    - useQuery goi getStudents({ ky_id: kyId, lop, gvhd_id: gvhdId, search, page })
    - useQuery goi getKyLvtn() de lay dropdown ky
    - useQuery goi getLecturers() de lay dropdown GVHD cho filter (SV-03)
    - useMutation cho import/create/update/delete, invalidate query on success

    Layout theo UI-SPEC:
    - Page heading "Sinh vien"
    - Toolbar row: Dropdown Ky LVTN, Dropdown Lop, Dropdown GVHD (optional, nullable — default "Tat ca GVHD"), Search input (placeholder "Tim theo MSSV, ho ten..."), Button "Import Excel" (primary), Button "Them sinh vien" (secondary)
    - Table dung TanStack Table: columns MSSV, Ho ten, Lop, Email, Actions (Sua, Xoa). Dung useReactTable voi getCoreRowModel, getPaginationRowModel. Pagination voi info "Hien thi {start}-{end} / {total} sinh vien"
    - Loading state: 5 skeleton rows animate-pulse
    - Empty state: centered "Chua co sinh vien" + sub-text

    Import Modal:
    - Overlay + modal card pattern (xem UI-SPEC)
    - Dropdown chon Ky LVTN (bat buoc)
    - File input accept=".xlsx,.xls"
    - Button "Import" disabled khi chua chon file/dang loading
    - Ket qua: success message (bg-green-50) + error table theo dong (bg-amber-50)

    Form Modal (them/sua SV):
    - Fields: MSSV (disabled khi sua), Ho ten, Lop, Email, Ky LVTN (dropdown)
    - Validation errors hien inline duoi moi field voi class `text-red-500 text-xs mt-1` — parse tu response.data.errors (Laravel 422 format: `{ errors: { field: ['message'] } }`)
    - KHONG dung window.alert() cho loi validation — chi hien inline duoi field
    - Buttons: Huy (secondary), Luu (primary)

    Delete Confirm Dialog:
    - Nho (max-w-sm), icon canh bao, text xac nhan, buttons Huy + Xoa (bg-red-500)

    Import icons tu react-icons/hi2: HiOutlineXMark, HiOutlineExclamationTriangle
  </action>
  <verify>
    <automated>cd frontend && grep "react-table" package.json && grep -l "getStudents" src/services/sinhVienService.js && grep -c "useQuery\|useMutation" src/pages/admin/SinhVien.jsx && grep "Content-Type" src/services/sinhVienService.js</automated>
  </verify>
  <done>Package react-table da cai, 3 service files tao xong (importStudents co Content-Type: undefined), trang SinhVien co table, import modal, CRUD modal, filter/search voi GVHD dropdown, pagination, delete confirm, validation errors inline</done>
</task>

<task type="auto" id="02-02-02">
  <name>Task 2: Trang GiangVien + Trang CaiDat Ky LVTN</name>
  <files>
    frontend/src/pages/admin/GiangVien.jsx,
    frontend/src/pages/admin/CaiDat.jsx
  </files>
  <read_first>
    - frontend/src/pages/admin/GiangVien.jsx -- hien tai la placeholder
    - frontend/src/pages/admin/CaiDat.jsx -- hien tai la placeholder
    - frontend/src/pages/admin/SinhVien.jsx -- pattern vua tao (Task 1), dung lam reference cho modal, table, error handling
    - frontend/src/services/giangVienService.js -- API functions
    - frontend/src/services/kyLvtnService.js -- API functions
    - .planning/phases/02-qu-n-l-sv-gv/02-UI-SPEC.md -- visual specs cho GiangVien va CaiDat
  </read_first>
  <action>
    A. Viet lai GiangVien.jsx tu placeholder thanh trang day du:

    State: showFormModal, editItem, deleteItem, showDeleteConfirm, search
    useQuery goi getLecturers(), queryKey ['giangvien']
    useMutation cho create/update/delete, invalidate ['giangvien'] on success

    Layout theo UI-SPEC:
    - Page heading "Giang vien"
    - Toolbar: search input (placeholder "Tim theo ten, email...") + button "Them giang vien" (primary)
    - Table (HTML table don gian — GV list nho, khong can TanStack): columns Ma GV, Ten GV, Email, Hoc vi, SV HD, DT PB, HD, Actions
    - Cot "SV HD" hien `{so_sv_hd}/10`. Khi so_sv_hd >= 10 thi text mau `text-amber-600 font-semibold`
    - GV co isAdmin hien badge "Admin" (bg-blue-50 text-blue-600) ben canh ten
    - Filter search: loc client-side theo tenGV hoac email
    - Loading/Empty states giong SinhVien pattern

    Form Modal (them/sua GV):
    - Fields: Ma GV (disabled khi sua), Ho ten, Email, Hoc vi (select: ThS, TS, PGS.TS, GS.TS), Mat khau (required khi them, optional khi sua — hint "De trong neu khong doi mat khau"), Checkbox "La Admin"
    - Validation errors hien inline duoi moi field — parse tu response.data.errors (Laravel 422 format), KHONG dung window.alert()
    - Buttons: Huy, Luu

    Delete Confirm:
    - Giong pattern SinhVien. Xu ly error 422 tu server (GV dang huong dan SV) — hien error message `bg-red-50 text-red-700`

    B. Viet lai CaiDat.jsx tu placeholder thanh trang quan ly ky LVTN:

    useQuery goi getKyLvtn(), queryKey ['kyLvtn']
    useMutation cho create/update, invalidate ['kyLvtn'] on success

    Layout theo UI-SPEC:
    - Page heading "Cai dat ky luan van"
    - Card tren: form tao/sua ky LVTN. Fields layout grid 2 cols. Ten ky (full width), 6 date inputs (native type="date"): Ngay bat dau, Ngay nhan de tai, Ngay cham 50%, Ngay phan bien, Ngay bao ve, Ngay ket thuc. Button "Luu thay doi" hoac "Tao ky moi" tuy context
    - Validation errors hien inline duoi field tuong ung — KHONG dung window.alert()
    - Card duoi: danh sach ky cu (table nho: Ten ky, Trang thai badge, Actions Sua). Click "Sua" load ky do vao form tren

    Flow:
    - Khi vao trang, neu da co ky active thi load vao form de sua
    - Neu chua co ky nao, form trong de tao moi
  </action>
  <verify>
    <automated>cd frontend && grep -c "useQuery\|useMutation" src/pages/admin/GiangVien.jsx && grep -c "useQuery\|useMutation" src/pages/admin/CaiDat.jsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Trang GiangVien co table voi counts, CRUD modal, search, delete confirm, inline validation errors. Trang CaiDat co form ky LVTN voi 6 date inputs va danh sach ky cu. Build thanh cong.</done>
</task>

<task type="checkpoint:human-verify" id="02-02-03" gate="blocking">
  <name>Task 3: Kiem tra toan bo giao dien Phase 2</name>
  <files>
    frontend/src/pages/admin/SinhVien.jsx,
    frontend/src/pages/admin/GiangVien.jsx,
    frontend/src/pages/admin/CaiDat.jsx
  </files>
  <action>
    Human verification — kiem tra 3 trang admin da build trong Task 1 va Task 2.

    Cac buoc kiem tra:
    1. Chay backend: `cd backend && php artisan serve`
    2. Chay frontend: `cd frontend && npm run dev`
    3. Dang nhap admin (admin@stu.edu.vn / 123456)

    Test Sinh vien:
    4. Vao /admin/sinh-vien -> thay danh sach SV (5 SV tu seeder)
    5. Click "Import Excel" -> chon file Excel mau -> click Import -> kiem tra SV moi xuat hien
    6. Thu filter theo lop, filter theo GVHD (dropdown), search theo ten
    7. Click "Them sinh vien" -> nhap thong tin -> de 1 field trong (vd: Ho ten) -> Luu -> kiem tra loi hien INLINE duoi field, KHONG phai alert
    8. Nhap day du -> Luu -> SV moi xuat hien trong bang
    9. Click "Sua" tren 1 SV -> doi ten -> Luu -> ten cap nhat
    10. Click "Xoa" -> confirm -> SV bi xoa

    Test Giang vien:
    11. Vao /admin/giang-vien -> thay danh sach GV (4 GV tu seeder)
    12. Kiem tra cot "SV HD" hien "0/10" (chua phan cong)
    13. Click "Them giang vien" -> de email trong -> Luu -> kiem tra loi inline
    14. Nhap day du -> Luu -> GV moi xuat hien
    15. Click "Sua" -> doi ten -> Luu
    16. Click "Xoa" -> confirm -> GV bi xoa (truong hop khong dang HD)

    Test Cai dat:
    17. Vao /admin/cai-dat -> thay form ky LVTN hien tai (HK2 2025-2026)
    18. Doi ten ky hoac ngay -> click "Luu thay doi" -> reload trang -> kiem tra du lieu con dung
  </action>
  <verify>User approved sau khi kiem tra toan bo UI flow. Type "approved" hoac mo ta issues can fix.</verify>
  <done>User xac nhan 3 trang admin hoat dong dung: import SV, CRUD SV/GV, cai dat ky LVTN, validation errors inline</done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | File upload tu client |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-FE-01 | Spoofing | Import modal | mitigate | File input chi accept .xlsx,.xls; server validate mime type |
| T-02-FE-02 | Info Disclosure | Service files | mitigate | Token tu localStorage, khong hardcode; api.js da co interceptor |

## Verification

```bash
cd frontend && npm run build
# Build thanh cong, khong loi

# Kiem tra khong con placeholder
grep -r "PlaceholderPage" frontend/src/pages/admin/SinhVien.jsx frontend/src/pages/admin/GiangVien.jsx frontend/src/pages/admin/CaiDat.jsx
# Khong co ket qua = OK
```

## Success Criteria

- 3 trang admin thay the placeholder thanh cong
- Import Excel UI hoat dong: chon file, upload, hien ket qua + loi theo dong
- Table SV co filter (lop, GVHD, ky), search, pagination
- Table GV co counts (so_sv_hd/10, so_dt_pb, so_hd)
- Form ky LVTN tao/sua voi 6 moc ngay
- Modal them/sua/xoa hoat dong cho ca SV va GV
- Validation errors hien inline duoi field, KHONG dung alert
- npm run build thanh cong

## Output

Sau khi hoan thanh, tao `.planning/phases/02-qu-n-l-sv-gv/02-02-SUMMARY.md`
